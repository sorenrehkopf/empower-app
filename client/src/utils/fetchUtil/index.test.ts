import { fetchRequest } from '.';

describe('when a body is given', () => {
	it('rattaches the body to to the fetch payload', () => {
		// TODO
	})
});

describe('when the fetch completes successfully', () => {
	it('returns the data as json', () => {
		// TODO
	})
});

describe('when the fetch does not successfully complete', () => {
	it('returns the error', () => {
		// TODO
	})
});

describe('when a non-fetch-related error occurs', () => {
	it('returns the error', () => {
		// TODO
	})
});
